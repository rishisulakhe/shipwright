apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: docker-dashboard
data:
  POSTGRES_DB: dockerdash
  POSTGRES_USER: dduser
  DATABASE_URL: postgres://dduser:ddpass@postgres-service:5432/dockerdash?sslmode=disable
  MIGRATIONS_PATH: /migrations
